import { render } from '@testing-library/react'

import { {{pascalCase componentName}} } from '../src'

describe('{{pascalCase componentName}}', () => {
  test('should render correctly', () => {
    const renderResult = render(<{{pascalCase componentName}} />)

    expect(() => renderResult.unmount()).not.toThrow()
  })
})
