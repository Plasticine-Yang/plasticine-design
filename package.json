{
  "private": true,
  "packageManager": "pnpm@7.15.0",
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "@plasticine-design/eslint-config": "workspace:*",
    "eslint": "^8.27.0",
    "prettier": "^2.7.1",
    "turbo": "^1.6.3"
  },
  "scripts": {
    "changeset:add": "changeset add",
    "changeset:version": "changeset version",
    "publish:eslint-config": "turbo run lint && pnpm run changeset:version && pnpm -C packages/eslint-config publish"
  }
}
